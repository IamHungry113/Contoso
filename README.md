# 1 install uv

https://docs.astral.sh/uv/getting-started/installation/#standalone-installer

example:

```sh
brew install uv
```

useing uv in docker

```sh
docker run --rm -it ghcr.io/astral-sh/uv:debian uv --help

```

# 2 install and start backend see backend readme.md

# 4 install and start frontend see backend readme.md
